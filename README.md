# ai-mesh
Decentralized P2P AI Network (Tor-Compatible, Local-First)  ai-mesh is a distributed AI runtime that allows independent nodes to run models locally, communicate over a peer-to-peer network, and optionally route traffic through Tor for privacy.
